# Inbox

The inbox is documented together with authentication and workspace access in
[`../auth/`](../auth/README.md).

The two were deliberately written as one document because they are one story: who you are,
what you are allowed to see, and what happens to a conversation once it reaches an agent.
Splitting them would have meant explaining workspace isolation twice.
